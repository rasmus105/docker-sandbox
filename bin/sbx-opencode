#!/usr/bin/env bash
set -euo pipefail

SANDBOX="opencode-$(basename "$PWD")"

if sbx ls 2>/dev/null | awk 'NR>1 {print $1}' | grep -qxF "$SANDBOX"; then
  exec sbx run "$SANDBOX"
else
  exec sbx run --template opencode-dev opencode .
fi
